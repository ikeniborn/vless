# VLESS + REALITY VPN Service

Защищенный VPN сервис на базе протокола VLESS с расширением REALITY для обхода DPI и блокировок.

## Возможности

- 🚀 Протокол VLESS с расширением REALITY
- 🐳 Развертывание в Docker
- 🛠 Интерактивное CLI управление
- 👥 Управление пользователями (до 50)
- 🔄 Автоматическое обновление Xray
- 💾 Резервное копирование и восстановление
- 📊 Мониторинг и логирование
- 🔑 Генерация QR-кодов для клиентов

## Системные требования

- **ОС:** Ubuntu 20.04+ / Debian 11+
- **RAM:** Минимум 512 MB (рекомендуется 1 GB)
- **CPU:** 1 vCPU
- **Диск:** 10 GB свободного места
- **Сеть:** Статический IP адрес, открытый порт 443
- **Docker:** 24.x
- **Docker Compose:** 2.x

## Быстрая установка

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/vless.git
cd vless

# Запуск установщика
sudo bash scripts/install.sh
```

Установщик выполнит:
1. Проверку системных требований
2. Установку Docker и зависимостей
3. Интерактивную настройку параметров
4. Генерацию криптографических ключей
5. Создание рабочей структуры в /opt/vless
6. Запуск Xray контейнера
7. Создание символических ссылок для команд

## Управление пользователями

### Интерактивное меню
```bash
vless-users
```

### Возможности управления пользователями
- **Показать список** - отображение всех пользователей с UUID и Short ID
- **Добавить пользователя** - автоматическая генерация UUID и Short ID
- **Удалить пользователя** - удаление с подтверждением
- **Экспортировать конфигурацию**:
  - vless:// ссылка для быстрого импорта
  - JSON конфигурация
  - QR-код в терминале (ASCII)
  - QR-код в PNG (640x640 пикселей)

## Обновление Xray

### Интерактивное меню
```bash
vless-update
```

Меню обновления предоставляет:
1. **Проверка обновлений** - сравнение текущей и последней версии
2. **Обновление Xray** - обновление с автоматическим бэкапом
3. **Принудительное обновление** - обновление даже если версия актуальна
4. **Откат версии** - восстановление из последней резервной копии
5. **Статус службы** - информация о состоянии контейнера
6. **Очистка Docker** - удаление неиспользуемых ресурсов

### Командная строка
```bash
# Проверить обновления
vless-update check

# Выполнить обновление
vless-update update

# Принудительное обновление
vless-update force

# Откатиться на предыдущую версию
vless-update rollback

# Проверить статус службы
vless-update status

# Очистить Docker ресурсы
vless-update clean
```

## Резервное копирование

### Интерактивное меню
```bash
vless-backup
```

Меню резервного копирования:
1. **Создать резервную копию** - полное резервное копирование системы
2. **Список резервных копий** - просмотр доступных бэкапов
3. **Восстановить** - восстановление из выбранной копии
4. **Очистка** - удаление старых резервных копий

### Командная строка
```bash
# Создать резервную копию
vless-backup create

# Показать список резервных копий
vless-backup list

# Восстановить из резервной копии
vless-backup restore

# Очистить старые резервные копии
vless-backup cleanup

# Автоматический режим (для скриптов)
vless-backup --auto
```

### Автоматическое резервное копирование
Резервные копии автоматически создаются:
- Перед обновлением Xray
- Перед восстановлением из другой резервной копии
- При откате версии

Система автоматически хранит последние 30 резервных копий.

### Содержимое резервной копии
- Конфигурационные файлы
- База данных пользователей
- Криптографические ключи X25519
- Скрипты управления
- Шаблоны
- Переменные окружения

## Работа с логами

### Интерактивное меню
```bash
vless-logs
```

Меню логов предоставляет:
1. **Показать логи** - просмотр последних N строк
2. **Следить за логами** - отслеживание в реальном времени
3. **Фильтрация логов**:
   - Только ошибки
   - Предупреждения и ошибки
   - События подключений
   - Поиск по тексту
4. **Экспорт логов** - сохранение в файл
5. **Статистика службы** - использование ресурсов
6. **Очистка логов** - удаление с предварительным бэкапом

### Командная строка
```bash
# Показать последние 100 строк логов
vless-logs show

# Следить за логами в реальном времени
vless-logs follow

# Экспортировать логи
vless-logs export

# Показать статистику
vless-logs stats

# Очистить логи
vless-logs clear
```

## Структура проекта

### Репозиторий
```
vless/
├── scripts/
│   ├── install.sh           # Установщик системы
│   ├── user-manager.sh      # Управление пользователями
│   ├── update.sh            # Обновление Xray (с меню)
│   ├── backup.sh            # Резервное копирование (с меню)
│   ├── logs.sh              # Работа с логами (с меню)
│   └── lib/
│       ├── colors.sh        # Цветной вывод
│       ├── utils.sh         # Общие функции
│       ├── config.sh        # Работа с конфигурацией
│       └── domains.sh       # Список целевых доменов
├── templates/               # Шаблоны конфигураций
├── docs/                    # Документация
└── README.md               # Этот файл
```

### Рабочий каталог (создается при установке)
```
/opt/vless/
├── config/                  # Конфигурации Xray
│   └── config.json         # Основная конфигурация
├── data/                    # Данные пользователей
│   ├── users.json          # База пользователей
│   ├── keys/               # Криптографические ключи
│   └── qr_codes/           # Сгенерированные QR-коды
├── backups/                # Резервные копии
├── logs/                   # Логи приложения
├── scripts/                # Копия скриптов управления
├── templates/              # Шаблоны
├── docker-compose.yml      # Docker Compose конфигурация
└── .env                    # Переменные окружения
```

## Безопасность

### Права доступа
Система автоматически устанавливает правильные права:
- Конфиденциальные файлы (ключи, .env): 600
- Конфигурации: 600
- Скрипты: 750
- Директории с данными: 700
- Публичные директории: 755

### Рекомендации безопасности
1. Регулярно обновляйте Xray через `vless-update`
2. Создавайте резервные копии перед важными изменениями
3. Используйте сложные Short ID для пользователей
4. Мониторьте логи на предмет аномалий
5. Ограничьте доступ к серверу по SSH
6. Используйте файрвол (ufw/iptables)
7. Регулярно проверяйте статус службы

## Клиентская настройка

### Поддерживаемые клиенты
- **Android:** v2rayNG, NekoBox
- **iOS:** Shadowrocket, Stash
- **Windows:** v2rayN, NekoRay
- **macOS:** V2RayU, Qv2ray
- **Linux:** v2ray-core, Qv2ray

### Методы подключения
1. **vless:// ссылка** - быстрый импорт в клиент
2. **QR-код** - сканирование камерой
3. **JSON конфигурация** - ручная настройка

### Получение конфигурации
```bash
# Запустите менеджер пользователей
vless-users

# Выберите "4) Экспортировать конфигурацию пользователя"
# Выберите пользователя
# Выберите метод экспорта
```

## Устранение неполадок

### Проверка статуса
```bash
# Статус контейнера
docker ps

# Детальный статус службы
vless-update status

# Просмотр логов в реальном времени
vless-logs follow
```

### Частые проблемы

**Контейнер не запускается:**
```bash
# Проверить логи Docker
cd /opt/vless
docker-compose logs xray-server

# Проверить конфигурацию
docker-compose config

# Валидация config.json
jq . /opt/vless/config/config.json
```

**Клиенты не подключаются:**
```bash
# Проверить порт 443
sudo netstat -tlnp | grep 443

# Проверить файрвол
sudo ufw status

# Открыть порт если нужно
sudo ufw allow 443/tcp

# Проверить логи подключений
vless-logs follow
```

**Недостаточно места на диске:**
```bash
# Очистить Docker ресурсы
vless-update clean

# Очистить старые бэкапы
vless-backup cleanup
```

**Восстановление после сбоя:**
```bash
# Восстановить из последней резервной копии
vless-backup restore
```

## Деинсталляция

```bash
# Остановить службу
cd /opt/vless
docker-compose down

# Удалить контейнеры и образы
docker system prune -a

# Создать финальную резервную копию (опционально)
vless-backup create

# Удалить файлы (ВНИМАНИЕ: удалит все данные!)
sudo rm -rf /opt/vless

# Удалить символические ссылки
sudo rm /usr/local/bin/vless-*
```

## Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение неполадок"
2. Просмотрите логи: `vless-logs`
3. Проверьте документацию в `docs/`
4. Создайте issue в репозитории

## Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## Авторы

Разработано для личного использования с поддержкой до 50 пользователей.

## Отказ от ответственности

Данный инструмент предоставляется только для образовательных и личных целей. Пользователи несут ответственность за соблюдение всех применимых законов и правил в своей юрисдикции.