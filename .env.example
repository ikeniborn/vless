# VLESS+Reality VPN Service Configuration Template
# Copy this file to .env and customize the values below

# Project settings
# Change PROJECT_PATH to match your installation directory
PROJECT_PATH=/home/ubuntu/vless
SCRIPT_VERSION=1.0.0

# Server configuration
# IMPORTANT: Replace with your server's actual public IP address
SERVER_IP=YOUR_SERVER_PUBLIC_IP
XRAY_PORT=443
REALITY_DEST=speed.cloudflare.com:443
REALITY_SERVER_NAMES=speed.cloudflare.com

# Logging
LOG_LEVEL=warning
LOG_FILE=/home/ubuntu/vless/logs/xray.log

# Database
USERS_DB=/home/ubuntu/vless/data/users.db
KEYS_DIR=/home/ubuntu/vless/data/keys

# Admin UUID (will be generated during installation if not set)
ADMIN_UUID=

# Docker configuration
DOCKER_IMAGE=teddysun/xray:latest
COMPOSE_PROJECT_NAME=vless-service

# ==============================================================
# CONFIGURATION NOTES:
# ==============================================================
#
# SERVER_IP: Your server's public IP address
#   - Auto-detected during installation
#   - Can be manually set if auto-detection fails
#
# XRAY_PORT: Port for VLESS+Reality service
#   - Default: 443 (HTTPS port for better bypass)
#   - Change only if port 443 is unavailable
#
# REALITY_DEST: Reality protocol destination
#   - Default uses Cloudflare for good performance
#   - Must support TLS 1.3 and HTTP/2
#
# REALITY_SERVER_NAMES: SNI names for Reality
#   - Should match REALITY_DEST domain
#   - Used for TLS handshake camouflage
#
# LOG_LEVEL: Xray logging verbosity
#   - Options: none, error, warning, info, debug
#   - Default: warning (balanced verbosity)
#
# DOCKER_IMAGE: Xray Docker image to use
#   - Default: teddysun/xray:latest
#   - Alternative: ghcr.io/xtls/xray-core:latest
#
# ==============================================================
# SECURITY RECOMMENDATIONS:
# ==============================================================
#
# 1. Keep this file secure (chmod 600 .env)
# 2. Never commit .env to version control
# 3. Use strong, unique values for keys and IDs
# 4. Regularly rotate private keys
# 5. Monitor logs for suspicious activity
#
# ==============================================================